# Test dependencies for the CI workflow. Pinned so a lint or test result can
# only change when this file changes. Dependabot raises a PR per new release,
# which is what keeps the rules tested against current ansible-lint.
ansible-lint==26.8.0
pytest==9.1.1
yamllint==1.38.0
